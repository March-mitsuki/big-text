export type I18nPageMetadata = {
  title: string;
  description: string;
};

export type I18nMetadata = {
  "/": I18nPageMetadata;
};

export type I18nText = {
  bigText: string;
  text: string;
  textColor: string;
  bgColor: string;
};
